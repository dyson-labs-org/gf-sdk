{
  "name": "gf-sdk-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test:unit": "npx -y tsx@latest --test tests/unit/**/*.test.ts",
    "test:integration": "npx -y tsx@latest --test tests/integration/phase4_execution.test.ts",
    "test": "pnpm -r test && pnpm test:unit && pnpm test:integration",
    "lint": "pnpm -r lint"
  }
}
